program letters;
var sentense:string;
var i, index: integer;
begin
length :=0;
index := 1;
writeln('Введите предложение');
readln(sentense);
writeln(sentense);
length := length(pred1);

while index <= length do begin
writeln(sentense[index]);
index := index + 1;
end;

end.
